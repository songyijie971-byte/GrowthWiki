# Codex 知识库维护规则

本文件适用于整个 Obsidian Vault。Codex 修改本知识库时，除用户当次明确要求外，遵守以下规则。

## 学习目标

默认目标不是闭卷默写或研究底层，而是帮助用户：

1. 大概理解一个概念解决什么问题。
2. 知道它通常怎么使用。
3. 能在项目代码中识别相关接口、角色和数据流。
4. 能参照现有代码完成小修改。

默认先达到“能看懂项目”的程度；只有用户主动要求时，才深入底层原理、复杂配置或从零独立编写。

## 学习顺序

讲解一个新知识点时，优先按以下顺序：

1. 用一句话说明用途。
2. 运行一个最小示例，看到真实效果。
3. 阅读一份短小完整的代码，指出关键入口和数据流。
4. 说明在真实项目中应该搜索哪些类、函数、接口或配置。
5. 需要时只做一个小修改，不立即要求从零默写完整代码。

如果用户暂时不会写代码，提供完整示例并带着阅读，不把闭卷编程作为前置条件。

## 知识库写入

- 优先扩充最接近的现有笔记，确认确实缺少独立概念后才新建。
- 可复用的原理写入概念卡片；通用命令流程写入 `10_ROS2/01_常用指令.md`；报错写入报错记录；项目事实写入小项目记录。
- 除非用户明确要求，否则不要创建学习日志、每日记录或课程验收笔记。
- 笔记保持简短，优先保留“用途、怎么用、代码怎么认、项目里怎么看”。
- 不重复堆放教程、长代码和相同解释；代码只保留理解概念所需的最小片段。
- 只有真实执行并确认过的结果才能标记为完成；尚未运行的内容必须明确标记为待验证。
- 不虚构文件、内部链接、运行结果、项目指标或用户没有说过的理解。

## 修改前后

- 修改前先检查学习地图、目标笔记、附近索引和现有链接，避免重复创建。
- 修改后重新读取或搜索具体改动，确认文件存在、链接有效、状态没有被误标。
- 用户当次指令始终优先；如果用户说内容太多，直接压缩到最小可用版本。

## 图谱同步

- 每次修改知识库后，默认检查 `核心知识图谱.canvas` 和 Obsidian 自动关系图是否需要同步。
- 新建、重命名、删除概念或模块，或者改变概念之间的属于、依赖、流程、通信、对比关系时，必须同步维护两张图。
- 自动关系图通过概念笔记的 `kg_*` 属性、内部链接和 `.obsidian/graph.json` 维护；只显示概念与领域节点，不加入 `Home`、`README`、日志、报错、模板和图片。
- `核心知识图谱.canvas` 只放已经建立的核心概念；新增节点时同时添加有名称的真实关系并调整布局，避免节点重叠和无意义连线。
- 单纯修改措辞、命令、状态、日志或报错时，只检查图谱一致性，不为了产生改动而强行增加节点或连线。
- 图谱修改后必须验证 Canvas 与 JSON 可解析、节点文件存在、旧路径没有残留；Obsidian 正在运行时再打开图谱做一次视觉检查。
